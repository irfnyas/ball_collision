name: ball_collision
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^2.18.0
  flutter: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flame: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
