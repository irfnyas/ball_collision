name: ball_collision
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^2.18.6
  flutter: ^3.3.10

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.7.3
  web_socket_client: ^0.1.0-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
